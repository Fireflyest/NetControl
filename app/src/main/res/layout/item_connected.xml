<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="150dp"
    android:layout_height="108dp"
    android:padding="16dp"
    android:layout_marginVertical="4dp"
    android:background="@drawable/round_white">

    <TextView
        android:id="@+id/item_connected_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:theme="@style/text_bold"/>

    <TextView
        android:id="@+id/item_connected_address"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@+id/item_connected_name"
        app:layout_constraintStart_toStartOf="@+id/item_connected_name"/>

    <ImageView
        android:id="@+id/item_connected_angle"
        android:layout_width="20dp"
        android:layout_height="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/ic_angle"
        android:contentDescription="@string/app_empty" />

</androidx.constraintlayout.widget.ConstraintLayout>